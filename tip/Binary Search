1. Tìm chính xác vị trí i = target
l, r := 0, len(nums)-1
for l <= r {
    mid := l + (r-l)/2
    if nums[mid] == target {
        return mid
    } else if nums[mid] < target {
        lo = mid + 1
    } else {
        hi = mid - 1
    }
}

2. Tìm vị trí gần nhất hoặc xa nhất so với target
l, r := 0, len(nums)-1
ans := 0
for l <= r {
    mid := l + (r-l)/2
    if nums[mid] <= target {  // thỏa điều kiện
        ans = mid               // ghi nhớ đáp án
        l = mid + 1            // thử tìm xa hơn bên phải
    } else {
        r = mid - 1            // quá lớn, thu hẹp bên trái
    }
}